import React, { Component } from 'react';
import Router from '../components/Router';

class MainLaout extends Component {
    state = {  }
    render() {
        return (
            <Router/>
         );
    }
}

export default MainLaout;
